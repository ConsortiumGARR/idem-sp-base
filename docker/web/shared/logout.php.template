<?php session_start();

if (isset($_SERVER['REMOTE_ADDR']) && !empty($_SERVER['REMOTE_ADDR'])) {
    echo '<div class="container d-flex justify-content-end">
            <button class="btn btn-danger btn-lg shadow-sm" onclick="window.location.href=\'/Shibboleth.sso/Logout?return=https://{{ .Env.SERVER_NAME }}\'">
                Logout
            </button>
          </div>';
    }
?>
